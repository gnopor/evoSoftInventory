export const APP_NAME = "EVOLUTIVE-SOFTWARE";

export const LANGUAGE_CODES = ["en", "fr"];
export const DEFAULT_LANGUAGE_CODE = LANGUAGE_CODES[1];

export const localStorageFields = {
    DEFAULT_USER_LANGUAGE: `x-${APP_NAME}-default-user-language`,

    INVENTORY_LIST: `x-${APP_NAME}-eventories`
};
